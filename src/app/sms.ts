export class Sms {
    phoneNumber?:String
    message?:String
    constructor(phoneNumber?:String,message?:String)
    {
        this.phoneNumber = phoneNumber
        this.message = message
    }
}
